use crate::runner::ds::execution_context::ExecutionContext;
use crate::runner::ds::lex_env::LexEnvironment;
use crate::runner::ds::realm::CodeRealm;
use std::rc::Rc;

pub fn script_initialization() {
    // let realm = CodeRealm::new();
    // let ctx = ExecutionContext {
    //     function: None,
    //     realm: Rc::new(realm),
    //     lex_env: LexEnvironment {
    //         inner: Box::new(()),
    //         outer: None,
    //     },
    //     var_env: LexEnvironment {},
    // };
}
